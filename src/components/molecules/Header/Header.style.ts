import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  headerContainer: {
    minWidth: 200,
    marginHorizontal: 16,
    height: 50,
    justifyContent: 'flex-end',
  },
  headerText: {
    color: 'black',
    fontWeight: '600',
    fontSize: 16,
  },
});
